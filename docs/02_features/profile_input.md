# 사주 프로필 입력 기능 명세서

> 만톡의 필수 기능 - 생년월일/시간 기반 사주 프로필 관리

---

## 1. 기본 정보

| 항목 | 내용 |
|------|------|
| 기능명 | 사주 프로필 입력/관리 |
| 우선순위 | **P0 (필수 - 핵심 기능)** |
| 라우트 | /profile/edit |
| 상태 | 기획 완료 |

---

## 2. 사용자 스토리

1. **사용자**로서, 내 생년월일과 시간을 입력해서 **사주 프로필을 생성**하고 싶다
2. **사용자**로서, 음력/양력을 **선택**하고 싶다
3. **사용자**로서, 출생 시간을 **모르면 "모름"**으로 표시하고 싶다
4. **사용자**로서, 여러 사람(나, 연인, 가족)의 **프로필을 관리**하고 싶다
5. **사용자**로서, 기존 프로필을 **수정/삭제**하고 싶다

---

## 3. 화면 구성

### 3.1 프로필 입력 화면 레이아웃
```
┌────────────────────────────────────────────┐
│ [←]    사주 정보 입력                       │  ← AppBar
├────────────────────────────────────────────┤
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 이 프로필의 이름                    │    │
│  │ [    나 / 연인 / 가족 / 직접입력    ]│    │  ← 프로필명
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 성별                               │    │
│  │ [  남성  ] [  여성  ]               │    │  ← 성별 선택
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 생년월일                           │    │
│  │ [1996] 년 [05] 월 [21] 일          │    │  ← 날짜 선택
│  │                                    │    │
│  │ [○ 양력] [○ 음력]                  │    │  ← 음양력
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 태어난 시간                         │    │
│  │ [09] 시 [30] 분                    │    │  ← 시간 선택
│  │                                    │    │
│  │ [□ 시간을 모릅니다]                 │    │  ← 시간 모름
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 태어난 곳 (선택)                    │    │
│  │ [  서울  ▼]                        │    │  ← 출생지 (선택)
│  └────────────────────────────────────┘    │
│                                            │
├────────────────────────────────────────────┤
│         [        저장하기        ]          │  ← 저장 버튼
└────────────────────────────────────────────┘
```

### 3.2 프로필 목록 화면 (설정 > 프로필 관리)
```
┌────────────────────────────────────────────┐
│ [←]    프로필 관리                          │
├────────────────────────────────────────────┤
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 👤 나                              │    │
│  │    1996.05.21 (양력) 09:30        │    │
│  │                         [수정] [X] │    │
│  └────────────────────────────────────┘    │
│                                            │
│  ┌────────────────────────────────────┐    │
│  │ 💕 연인                            │    │
│  │    1995.11.03 (음력) 시간모름      │    │
│  │                         [수정] [X] │    │
│  └────────────────────────────────────┘    │
│                                            │
│        [  + 새 프로필 추가  ]              │
│                                            │
└────────────────────────────────────────────┘
```

### 3.3 UI 요소

| 요소 | 타입 | 동작 |
|------|------|------|
| 프로필명 입력 | ChoiceChip + TextField | 프리셋 선택 또는 직접 입력 |
| 성별 선택 | SegmentedButton | 남성/여성 선택 (필수) |
| 생년월일 선택 | DatePicker / 스크롤 휠 | 년/월/일 선택 |
| 음양력 선택 | Radio | 양력(기본) / 음력 |
| 출생시간 선택 | TimePicker / 스크롤 휠 | 시/분 선택 |
| 시간 모름 체크 | Checkbox | 체크 시 시간 입력 비활성화 |
| 출생지 선택 | Dropdown | 주요 도시 목록 (선택) |
| 저장 버튼 | ElevatedButton | 모든 필수값 입력 시 활성화 |

---

## 4. 수락 조건 (Acceptance Criteria)

### 4.1 프로필 생성
- [ ] 프로필명을 입력할 수 있다 (프리셋: 나, 연인, 가족 / 직접입력)
- [ ] 성별을 선택할 수 있다 (필수)
- [ ] 생년월일을 선택할 수 있다 (필수)
- [ ] 음력/양력을 선택할 수 있다 (기본: 양력)
- [ ] 출생시간을 입력할 수 있다 (선택)
- [ ] "시간을 모릅니다" 체크 시 시간 입력이 비활성화된다
- [ ] 출생지를 선택할 수 있다 (선택)
- [ ] 저장 시 로컬(Hive)에 저장된다

### 4.2 프로필 수정
- [ ] 기존 프로필을 수정할 수 있다
- [ ] 수정 화면 진입 시 기존 값이 채워져 있다

### 4.3 프로필 삭제
- [ ] 프로필을 삭제할 수 있다
- [ ] 삭제 전 확인 다이얼로그를 표시한다
- [ ] 마지막 프로필은 삭제할 수 없다 (최소 1개 필요)

### 4.4 프로필 전환
- [ ] 채팅 화면에서 프로필을 전환할 수 있다
- [ ] 프로필 전환 시 해당 프로필 기반으로 AI 상담 진행

### 4.5 유효성 검사
- [ ] 생년월일은 1900년~현재까지만 허용
- [ ] 시간은 00:00~23:59 범위
- [ ] 필수값(이름, 성별, 생년월일) 미입력 시 저장 버튼 비활성화

---

## 5. UI/UX 흐름

### 5.1 신규 프로필 생성 흐름
```
온보딩 완료 (or 프로필 추가 탭)
    ↓
프로필 입력 화면 (/profile/edit)
    ↓
필수 정보 입력
 - 프로필명
 - 성별
 - 생년월일 + 음양력
 - 출생시간 (선택)
    ↓
[저장하기] 탭
    ↓
로컬 저장 (Hive)
    ↓
사주 챗봇 화면 이동 (/saju/chat)
```

### 5.2 프로필 수정 흐름
```
설정 > 프로필 관리
    ↓
프로필 카드 [수정] 탭
    ↓
프로필 입력 화면 (기존 값 채움)
    ↓
수정 후 [저장하기]
    ↓
업데이트 완료 → 목록으로 복귀
```

### 5.3 프로필 삭제 흐름
```
설정 > 프로필 관리
    ↓
프로필 카드 [X] 탭
    ↓
확인 팝업 "정말 삭제하시겠습니까?"
    ├─ [삭제] → 프로필 삭제
    └─ [취소] → 팝업 닫기
```

---

## 6. 예외 처리

| 상황 | 처리 방법 | UI 표시 |
|------|-----------|---------|
| 필수값 미입력 | 저장 버튼 비활성화 | 비활성화 상태 |
| 유효하지 않은 날짜 | 입력 불가 | DatePicker 범위 제한 |
| 마지막 프로필 삭제 시도 | 삭제 불가 | "최소 1개의 프로필이 필요합니다" |
| 로컬 저장 실패 | 재시도 옵션 | "저장 실패. 다시 시도해주세요" |
| 중복 프로필명 | 허용 (동일 이름 가능) | - |

---

## 7. 데이터 요구사항

### 7.1 입력 데이터
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| displayName | String | O | 프로필 이름 (나, 연인 등) |
| gender | Gender | O | 성별 (male/female) |
| birthDate | DateTime | O | 생년월일 |
| isLunar | bool | O | 음력 여부 (기본: false) |
| birthTimeMinutes | int? | X | 출생시간 (분 단위, 0~1439) |
| birthTimeUnknown | bool | O | 시간 모름 여부 (기본: false) |
| birthPlace | String? | X | 출생지 (선택) |

### 7.2 저장 위치 (Supabase)
- **서버**: Supabase `saju_profiles` 테이블 (RLS 적용)
- **로컬 캐시**: Hive Box (`saju_profiles_cache`) - 오프라인 지원용

### 7.3 Supabase 쿼리 예시
```dart
// 프로필 생성
final profile = await supabase
    .from('saju_profiles')
    .insert({
      'display_name': displayName,
      'birth_date': birthDate.toIso8601String().split('T')[0],
      'birth_time_minutes': birthTimeMinutes,
      'birth_time_unknown': birthTimeUnknown,
      'is_lunar': isLunar,
      'gender': gender.name,
      'birth_place': birthPlace,
    })
    .select()
    .single();

// 프로필 목록 조회
final profiles = await supabase
    .from('saju_profiles')
    .select()
    .order('created_at', ascending: false);

// 프로필 수정
await supabase
    .from('saju_profiles')
    .update({'display_name': newName})
    .eq('id', profileId);

// 프로필 삭제
await supabase
    .from('saju_profiles')
    .delete()
    .eq('id', profileId);

// 활성 프로필 설정 (다른 프로필은 비활성화)
await supabase.rpc('set_active_profile', params: {'profile_id': profileId});
```

### 7.4 데이터 모델 참조
```dart
// features/profile/domain/entities/saju_profile.dart
class SajuProfile {
  final String id;
  final String displayName;
  final DateTime birthDate;
  final int? birthTimeMinutes;
  final bool birthTimeUnknown;
  final bool isLunar;
  final Gender gender;
  final String? birthPlace;
  final DateTime createdAt;
  final DateTime updatedAt;
}
```

---

## 8. 상태 관리

### 8.1 ProfileState
```dart
class ProfileState {
  final List<SajuProfile> profiles;
  final SajuProfile? activeProfile;
  final bool isLoading;
  final String? error;
}
```

### 8.2 ProfileFormState (입력 폼 상태)
```dart
class ProfileFormState {
  final String displayName;
  final Gender? gender;
  final DateTime? birthDate;
  final bool isLunar;
  final int? birthTimeMinutes;
  final bool birthTimeUnknown;
  final String? birthPlace;
  final bool isValid;
}
```

### 8.3 이벤트/액션
| 액션 | 설명 |
|------|------|
| loadProfiles | 저장된 프로필 목록 로드 |
| createProfile | 새 프로필 생성 |
| updateProfile | 프로필 수정 |
| deleteProfile | 프로필 삭제 |
| setActiveProfile | 활성 프로필 설정 |
| validateForm | 폼 유효성 검사 |

---

## 9. 의존성

### 9.1 다른 기능과의 관계
- **후행**: saju_chart (프로필 기반 만세력 계산)
- **후행**: saju_chat (프로필 기반 AI 상담)
- **연관**: settings (프로필 관리 진입점)

### 9.2 외부 패키지
- `hive_flutter` (로컬 저장)
- `flutter_riverpod` (상태 관리)

---

## 10. 테스트 케이스

| TC ID | 시나리오 | 예상 결과 |
|-------|----------|-----------|
| PROF-001 | 필수값 모두 입력 후 저장 | 프로필 생성 성공 |
| PROF-002 | 시간 모름 체크 | 시간 입력 필드 비활성화 |
| PROF-003 | 음력 선택 | isLunar = true로 저장 |
| PROF-004 | 기존 프로필 수정 | 업데이트 성공 |
| PROF-005 | 프로필 삭제 | 삭제 확인 후 삭제 |
| PROF-006 | 마지막 프로필 삭제 시도 | 삭제 불가 안내 |
| PROF-007 | 필수값 누락 | 저장 버튼 비활성화 |
| PROF-008 | 프로필 전환 | 활성 프로필 변경 |

---

## 11. 디자인 참고

### 11.1 프로필명 프리셋
```dart
const profileNamePresets = [
  '나',
  '연인',
  '가족',
  '친구',
];
```

### 11.2 주요 도시 목록 (출생지)
```dart
const birthPlaceOptions = [
  '서울', '부산', '대구', '인천', '광주',
  '대전', '울산', '세종', '경기', '강원',
  '충북', '충남', '전북', '전남', '경북',
  '경남', '제주', '해외',
];
```

---

## 변경 이력

| 날짜 | 버전 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 2025-12-01 | 0.1 | 초안 작성 | - |
